# Firebase Configuration
# Copy this file to .env and fill in with your actual Firebase credentials from the Firebase Console.
# DO NOT commit .env with real credentials — it's excluded in .gitignore.
# Obtain your API key from: Firebase Console → Project Settings → Web API Key

FIREBASE_API_KEY=your_firebase_api_key_here
